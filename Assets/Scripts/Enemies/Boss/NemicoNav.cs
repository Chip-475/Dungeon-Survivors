using UnityEngine;
using UnityEngine.AI;
public class NemicoNav : MonoBehaviour
{/*
    NavMeshAgent agent;
    Transform player;

    void Awake()
    {
        agent = GetComponent<NevMeshAgent>();
    }

    void Start()
    {
        player = GameObject.FindObjectWithTag("Player")?.Transform;
    }

    void Update()
    {
        if (player == null) return;
        agent.SetDestination(player.position);
    }*/
}
